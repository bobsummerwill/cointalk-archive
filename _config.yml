# GitHub Pages serves pre-built static HTML from the docs/ directory.
# No Jekyll processing is needed.
# Configure GitHub Pages: Settings > Pages > Source: "Deploy from a branch" > Branch: main, folder: /docs
